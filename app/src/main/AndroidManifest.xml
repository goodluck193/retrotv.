<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Приложение для ТВ: пульт/геймпад, тачскрин не обязателен -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.gamepad" android:required="false" />

    <!-- НИКАКИХ разрешений на всю память: ромы выбираются через системный
         файловый диалог (SAF) и копируются в приватную папку приложения.
         Это самый безопасный для ТВ способ. -->

    <application
        android:label="${appLabel}"
        android:icon="@drawable/app_icon"
        android:banner="@drawable/tv_banner"
        android:extractNativeLibs="true"
        android:theme="@style/Theme.RetroTV">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".GameActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboard|keyboardHidden|navigation" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false" />
    </application>
</manifest>
